import 'package:stacked/stacked.dart';

class ProcessingDataViewModel extends BaseViewModel {

  /// Al llamar a esta vista (widget), se le pasa el titulo que mostrar
  ProcessingDataViewModel(this.title);
  
  String title = '';



}